apiVersion: v1
kind: Secret
metadata:
  name: sluice-secrets
  namespace: sluice
type: Opaque
stringData:
  # WARNING: This is an EXAMPLE file. Replace the placeholder before applying.
  #
  # DO NOT commit real secrets to git. Create the secret with:
  #   kubectl create secret generic sluice-secrets \
  #     --namespace sluice \
  #     --from-literal=umans-api-key='sk-...'
  #
  # For future external exposure, add an admin token to the same secret:
  #     --from-literal=admin-token="$(openssl rand -hex 32)"
  # then uncomment the SLUICE_ADMIN_TOKEN block in deployment.yaml.
  umans-api-key: "REPLACE-ME-UMANS-API-KEY"
  # admin-token: "REPLACE-ME-ADMIN-TOKEN"   # optional; only needed when exposing externally
